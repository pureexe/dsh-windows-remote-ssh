# Copy to .env (or export directly) to run the live integration suite:
#   SSH_HOST=... SSH_USER=... SSH_PASSWORD=... npm run test:e2e

SSH_HOST=192.0.2.10
SSH_PORT=22
SSH_USER=change-me
SSH_PASSWORD=change-me
# Prefer a key over a password where you can:
# SSH_KEY_PATH=/path/to/id_ed25519
# SSH_KEY_PASSPHRASE=
